document.querySelectorAll(".menu").forEach((button)=>{const nav=button.nextElementSibling;button.addEventListener("click",()=>{const open=nav.classList.toggle("open");button.setAttribute("aria-expanded",String(open));button.setAttribute("aria-label",open?"Cerrar menú":"Abrir menú")});nav.querySelectorAll("a").forEach((link)=>link.addEventListener("click",()=>{nav.classList.remove("open");button.setAttribute("aria-expanded","false")}));});
document.querySelectorAll("[data-reservation-form]").forEach((form)=>form.addEventListener("submit",(event)=>{event.preventDefault();const name=form.querySelector("[name=name]").value||"";form.querySelector("[data-message]").textContent=`${name?`Gracias, ${name}. `:""}Llámanos al 968 54 84 98 para confirmar disponibilidad.`;}));
