"use client";
import { useEffect, useState } from "react";
export function CookieBanner(){const [open,setOpen]=useState(false);useEffect(()=>setOpen(!localStorage.getItem("l3p-cookies")),[]);if(!open)return null;const choose=(value:string)=>{localStorage.setItem("l3p-cookies",value);setOpen(false)};return <aside className="cookie-bar" aria-label="Preferencias de cookies"><strong>Tu privacidad</strong><p>Solo usamos las tecnologías imprescindibles hasta que elijas una preferencia.</p><div className="cookie-actions"><button className="accept" onClick={()=>choose("accepted")}>Aceptar</button><button onClick={()=>choose("rejected")}>Rechazar</button><button onClick={()=>choose("essential")}>Configurar</button></div></aside>}
