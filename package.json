{
  "name": "los-tres-pacos-web",
  "private": true,
  "type": "module",
  "description": "Web pública de Los Tres Pacos (Portmán). Sin dependencias.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/dev.mjs",
    "check": "node scripts/build.mjs && node scripts/check.mjs"
  }
}
